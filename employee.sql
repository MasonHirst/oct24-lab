1. select last_name, first_name, city from employee
    where city = 'Calgary' ;

2. select * from employee
    order by birth_date DESC
    Limit 1;

3. select * from employee
    order by birth_date
    Limit 1;

4.  nancy is 2